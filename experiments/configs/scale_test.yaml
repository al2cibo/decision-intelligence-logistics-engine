experiment_name: scale_test
dataset_path: experiments/datasets/synthetic_v2
output_path: experiments/results/scale_test

per_destination_forecasting:
  model_names:
    - naive_forecaster
    - seasonal_forecaster
    - rolling_window_forecaster
    - ets_forecaster
    - sarimax_forecaster
  train_ratio: 0.8
  selection_metric: wape
  minimum_history_length: 10
  random_seed: 42
  max_workers: 4
  model_params:
    seasonal_forecaster:
      lag_value: 7
    rolling_window_forecaster:
      rolling_window: 7
