experiment_name: baseline_naive
dataset_path: experiments/datasets/synthetic_v1
output_path: experiments/results/baseline_naive

per_destination_forecasting:
  model_names:
    - naive_forecaster
  train_ratio: 0.8
  selection_metric: wape
  minimum_history_length: 10
  random_seed: 42
